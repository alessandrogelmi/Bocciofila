import static org.junit.Assert.assertTrue;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.Test;

class TestCaseNodo 
{

	/*@Test
	public void testCostruttoreNodo() 
	{
		Tessera tessera=new Tessera();
		Nodo n=new Nodo(tessera);
		assertTrue("Costruttore nodo", n.getInfo().equals(tessera) && n.getLink()==null);
	}*/
	
	/*@Test
	public void testGetInfo()
	{
		Tessera t1=new Tessera();
		Tessera t2=new Tessera();
		Nodo n=new Nodo(t1);
		n.setInfo(t2);
		assertTrue("getInfo", n.getInfo().equals(t2) && n.getLink()==null);
	}*/
	
	/*@Test
	public void testGetLink()
	{
		Tessera t1=new Tessera();
		Tessera t2=new Tessera();
		Nodo n1=new Nodo(t1);
		Nodo n2=new Nodo(t2);
		n1.setLink(n2.getLink());
		assertTrue("getLink", n1.getInfo().equals(t1) && n1.getLink()==n2.getLink());
	
	}*/
	
	/*@Test
	public void testSetInfo() 
	{
		Tessera t1=new Tessera();
		Tessera t2=new Tessera();
		Nodo n=new Nodo(t1);
		n.setInfo(t2.getInfo());
		assertTrue("setInfo", n.getInfo().equals(t2) && n.getLink()==null);
	}*/
	
	/*@Test
	public void testSetLink() 
	{
		Tessera t1=new Tessera();
		Tessera t2=new Tessera();
		Nodo n1=new Nodo(t1);
		Nodo n2=new Nodo(t2);
		n1.setLink(n2);
		assertTrue("setLink", n1.getInfo().equals(t1) && n1.getLink()==n2);
	}*/
	
	
}
